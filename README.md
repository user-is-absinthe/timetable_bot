# 📚 Telegram Бот для Напоминаний о Занятиях

## 🚀 Установка и Настройка

### 1. Установка зависимостей

```bash
pip install python-telegram-bot pytz
```

Требуемые версии: 
- `python-telegram-bot >= 20.0`
- `pytz >= 2023.3`

### 2. Получение Bot Token

1. Напиши боту [@BotFather](https://t.me/botfather) в Telegram
2. Отправь команду `/newbot`
3. Следуй инструкциям и получи токен
4. Скопируй токен и вставь его в переменную `TOKEN` в коде

### 3. Структура проекта

```
project/
├── timetable_bot.py      # Основной файл бота
├── timetable.csv         # Расписание (создать самостоятельно)
└── reminders.json        # Будет создан автоматически
```

### 4. CSV Файл с Расписанием

Создай файл `timetable.csv` в кодировке UTF-8 с разделителем `;`:

```
Дата;Пара;Дисциплина;Номер темы;Вид занятия;Преподаватели;Ауд.
26.01.2026;1;Русский язык;1.1;л;Иванов В.В.;403(З)
26.01.2026;2;Русский язык;1.2;л;Иванов В.В.;403(З)
26.01.2026;3;Русский язык;2.1;л;Иванов В.В.;403(З)
26.01.2026;4;;Разработка;диссертации;;
29.01.2026;1;;Разработка;диссертации;;
29.01.2026;2;;Разработка;диссертации;;
29.01.2026;3;;Разработка;диссертации;;
29.01.2026;4;;Разработка;диссертации;;
```

**Важно:**
- Дата в формате: `ДД.МММ.ГГГГ` (например: `26.01.2026`)
- Разделитель: `;` (точка с запятой)
- Кодировка: UTF-8
- Пустые поля можно оставлять пустыми (например, для самоподготовки)
- День только самоподготовки - когда все дисциплины пусты

### 5. ⏰ ВАЖНО: Настройка Часового Пояса

В начале файла `timetable_bot.py` найди эту строку:

```python
TIMEZONE = pytz.timezone('Europe/Moscow')  # МСК
```

**Установи свой часовой пояс из списка:**

| Город/Регион | Код |
|---|---|
| Москва (МСК) | `Europe/Moscow` |
| Стамбул | `Europe/Istanbul` |
| Лондон | `Europe/London` |
| Берлин | `Europe/Berlin` |
| Нью-Йорк | `America/New_York` |
| Лос-Анджелес | `America/Los_Angeles` |
| Токио | `Asia/Tokyo` |
| Сидней | `Australia/Sydney` |
| UTC (Гринвич) | `UTC` |

**Полный список часовых поясов:**
```bash
python -c "import pytz; print('\n'.join(pytz.all_timezones))"
```

**Пример для другого пояса:**
```python
TIMEZONE = pytz.timezone('Europe/Berlin')  # Берлин
```

### 6. Получение Chat ID

1. Запусти бот первый раз
2. Отправь команду `/start` боту
3. Бот выведет сообщение с "Chat ID (для конфига): ЧИСЛO"
4. Скопируй это число (должно быть отрицательным, например: `-1001234567890`)
5. Вставь его в переменную `CHAT_ID` в коде

### 7. Запуск бота

```bash
python timetable_bot.py
```

---

## 📋 Функциональность

### Автоматические Напоминания

- **7:30** (твой часовой пояс) - напоминание о парах на сегодня
- **19:30** (твой часовой пояс) - напоминание о парах на завтра (если завтра учебный день)

### Команды в Чате

| Команда | Описание |
|---------|----------|
| `/start` | Запуск бота |
| `/get_timetable [01.02.2026]` | Получить расписание на дату (или на следующий учебный день) |
| `/set_reminder "текст"` | Добавить напоминание |

### Кнопки Меню

- 📅 **Расписание** - выбрать дату и показать расписание
- ⏰ **Мои напоминания** - посмотреть установленные напоминания
- ➕ **Добавить напоминание** - добавить новое напоминание
- 🗑️ **Удалить напоминание** - удалить напоминание (или выбрать одно из нескольких)

### Напоминания Пользователей

**Каждый пользователь может установить НЕСКОЛЬКО напоминаний!**

При отправке расписания напоминания показываются в формате:

**Если у пользователя одно напоминание:**
```
📚 Расписание на 26.01.2026:
1. Русский язык (1.1) | л | Иванов В.В. | 403(З)
2. Русский язык (1.2) | л | Иванов В.В. | 403(З)

@Иван:
"Подготовить доклад"

@Мария:
"Принести тетрадь"
```

**Если у пользователя несколько напоминаний:**
```
📚 Расписание на 26.01.2026:
1. Русский язык (1.1) | л | Иванов В.В. | 403(З)

@Иван:
1. "Подготовить доклад"
2. "Купить учебник"
3. "Отправить эссе"

@Мария:
1. "Напомнить про консультацию"
2. "Принести справку"
```

---

## 🔧 Конфигурация

Отредактируй переменные в начале файла `timetable_bot.py`:

```python
CHAT_ID = -1001234567890  # ID чата (получить при первом запуске)
TOKEN = "YOUR_BOT_TOKEN"  # Токен от @BotFather
CSV_FILE = "timetable.csv"  # Путь к CSV файлу

# ЧАСОВОЙ ПОЙ АС - ОЧЕНЬ ВАЖНО!
TIMEZONE = pytz.timezone('Europe/Moscow')  # МСК (или свой пояс)

REMINDER_MORNING_TIME = (7, 30)    # Время утреннего напоминания
REMINDER_EVENING_TIME = (19, 30)   # Время вечернего напоминания
```

---

## 📝 Примеры Использования

### Пример 1: Добавление Нескольких Напоминаний

```
/set_reminder "Подготовить доклад к семинару"
```

Ответ:
```
✅ Напоминание добавлено: 'Подготовить доклад к семинару'

У тебя 1 напоминание
```

```
/set_reminder "Купить тетрадь"
```

Ответ:
```
✅ Напоминание добавлено: 'Купить тетрадь'

У тебя 2 напоминания
```

### Пример 2: Просмотр Напоминаний

Нажми кнопку "⏰ Мои напоминания" или отправь её текстом.

Ответ:
```
📌 Твои напоминания:

1. "Подготовить доклад к семинару"
2. "Купить тетрадь"
```

### Пример 3: Удаление Напоминания

Нажми "🗑️ Удалить напоминание"

Бот выведет:
```
🗑️ Какое напоминание удалить?

1. "Подготовить доклад к семинару"
2. "Купить тетрадь"

Отправь номер (например: 2) или 'все' для удаления всех
```

Отправь `2`:
```
🗑️ Удалено: "Купить тетрадь"
```

### Пример 4: Расписание с Несколькими Напоминаниями

```
/get_timetable 26.01.2026
```

Ответ:
```
📚 Расписание на 26.01.2026:

1. Русский язык (1.1) | л | Иванов В.В. | 403(З)
2. Русский язык (1.2) | л | Иванов В.В. | 403(З)
3. Русский язык (2.1) | л | Иванов В.В. | 403(З)
4. Разработка диссертации

@Иван:
1. "Подготовить доклад"
2. "Купить учебник"

@Мария:
"Принести тетрадь"
```

---

## 🐛 Решение Проблем

### Ошибка: "ModuleNotFoundError: No module named 'telegram'"

**Решение:**
```bash
pip install python-telegram-bot pytz
```

### Ошибка: "ModuleNotFoundError: No module named 'pytz'"

**Решение:**
```bash
pip install pytz
```

### Ошибка: "Файл timetable.csv не найден"

**Решение:**
1. Создай файл `timetable.csv` в той же папке, что и скрипт
2. Убедись, что имя файла и путь в конфиге совпадают

### Бот не отправляет автоматические напоминания

**Решение:**
1. Проверь, что бот работает (`python timetable_bot.py`)
2. Убедись, что правильно задан `CHAT_ID`
3. **Проверь часовой пояс** - убедись, что `TIMEZONE` совпадает с твоей локацией
4. Посмотри в консоли логи при запуске - должно быть "⏰ Часовой пояс: Europe/Moscow"
5. Убедись, что в CSV есть пары на эту дату

### Напоминания не появляются в расписании

**Решение:**
1. Проверь, что пользователь установил напоминание (`/set_reminder`)
2. Убедись, что дата еще не была объявлена в этот день
3. После первого объявления напоминания **НЕ удаляются** автоматически - пользователь может добавлять новые

### Неправильное время напоминаний

**Решение:**
1. Проверь переменную `TIMEZONE` - должна совпадать с твоим часовым поясом
2. Проверь `REMINDER_MORNING_TIME` и `REMINDER_EVENING_TIME`
3. Сервер должен быть синхронизирован по времени

---

## 📌 Важные Замечания

1. **Часовой пояс**: Установи правильный часовой пояс через переменную `TIMEZONE`. Это критически важно для корректной работы автоматических напоминаний!

2. **Рабочие дни**: По умолчанию рабочие дни - это пн-пт (выходные сб-вс).

3. **Несколько напоминаний**: Каждый пользователь может установить несколько напоминаний. Они все будут выводиться при отправке расписания.

4. **CSV Кодировка**: Используй кодировку UTF-8, иначе русские символы будут отображаться некорректно.

5. **Безопасность**: Не публикуй токен бота в интернет!

6. **Администраторские права**: Убедись, что бот добавлен как администратор в чат.

---

## 📞 Поддержка

Если возникнут вопросы, проверь:
- Документацию [python-telegram-bot](https://python-telegram-bot.readthedocs.io/)
- Логи в консоли (они выведут детали ошибок)
- Что бот добавлен администратором в чат
- Что правильно установлен часовой пояс

Удачи! 🚀
